spring:
    datasource:
      url: jdbc:postgresql://37.18.100.40:5432/prom
      password: changeme
      username: postgres
    jpa:
      hibernate:
        ddl-auto: create-drop
      properties:
        hibernate:
          show_sql: true
          format_sql: true
          default_schema: prom
    logging:
      level:
        org:
          hibernate:
            type: trace
#logging:
#  level:
#    org.springframework.security: DEBUG
